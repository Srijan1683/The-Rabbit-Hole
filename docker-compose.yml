services:
  db:
    image: postgres:16
    container_name: rabbithole-db
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - ./migrations/001_initial_schema.sql:/docker-entrypoint-initdb.d/init.sql